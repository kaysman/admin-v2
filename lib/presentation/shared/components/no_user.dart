import 'package:flutter/material.dart';

class NoIdentityScreen extends StatelessWidget {
  const NoIdentityScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('No Identity (user)'),
      ),
    );
  }
}

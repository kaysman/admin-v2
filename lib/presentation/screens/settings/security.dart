import 'package:flutter/material.dart';

class SecurityPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "To be released screen",
        style: Theme.of(context).textTheme.bodyText1,
      ),
    );
  }
}
